import {NextFunction, Request, Response} from "express";

//TO DO: responses to one format by req methods
const responseFormater = function (req: Request, res: Response, next: NextFunction) {
}

export default responseFormater;